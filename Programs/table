#include<stdio.h>
#include<math.h>
#include<string.h>
int main()
{
	int n;
	printf("Enter any number for their table:");
	scanf("%d",&n);
	for(int i=1 ; i<=10 ; i=i+1)
	{
		printf("%d\n",n*i);
	}
	return 0;
}
