#include<stdio.h>
int main()
{
    FILE *fptr;
    fptr=fopen("hello.txt","r");
    char ch=fgetc(fptr);
    while(ch!=EOF)
    {
        printf("%c",ch);
        ch=fgetc(fptr);
    } 
    fclose(ptr);
    return 0;
  }
